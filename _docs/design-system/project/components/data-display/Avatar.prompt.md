Circular avatar; shows the image, or initials tinted with the module accent when no `src`.

```jsx
<Avatar name="Riley Chen" src="/me.jpg" size="md" />
<Avatar name="Sam Okafor" size="sm" />
```

Sizes: `xs` 24 · `sm` 32 · `md` 40 · `lg` 56.
