A binary on/off toggle — use for settings like "Repeat alarm" or "Vibrate". The thumb springs across; the track fills with the active module accent.

```jsx
<Switch defaultChecked aria-label="Repeat" />
<Switch checked={on} onChange={e => setOn(e.target.checked)} />
```
